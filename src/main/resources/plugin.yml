name: PigStops
version: '${project.version}'
main: me.pigalala.pigstops.PigStops
api-version: 1.19
prefix: PigStops
authors: [ Pigalala ]
depend: [ TimingSystem ]

commands:
  pigstop:
    aliases: []

permissions:
  pigstop.modify:
    description: "Allows the player to modify the plugin through commands"
    default: op